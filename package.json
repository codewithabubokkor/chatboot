{
    "name": "my-chatbot-project",
    "version": "1.0.0",
    "description": "A simple chatbot using Groq",
    "scripts": {
        "dev": "vercel dev",
        "build": "vercel build",
        "deploy": "vercel --prod"
    },
    "dependencies": {
        "dotenv": "^10.0.0",
        "groq-sdk": "^0.12.0"
    },
    "devDependencies": {
        "vercel": "^28.18.5"
    },
    "engines": {
        "node": "22.x"
    }
}
