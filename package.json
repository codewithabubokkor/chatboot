{
    "name": "my-chatbot-project",
    "version": "1.0.0",
    "description": "A simple chatbot using Groq",
    "scripts": {
        "dev": "vercel dev",
        "build": "vercel build",
        "deploy": "vercel --prod"
    },
    "dependencies": {
        "groq-sdk": "^1.0.0",
        "dotenv": "^10.0.0"
    },
    "devDependencies": {
        "vercel": "^23.0.0"
    },
    "engines": {
        "node": "22.x"
    }
}